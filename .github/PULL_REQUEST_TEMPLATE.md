## What this changes

<!-- One or two sentences. What is different for a person using Rank & Folder? -->

## Why

<!-- The problem this solves. Link an issue if one exists. -->

## Checklist

- [ ] `swift test` passes
- [ ] The unsigned app build passes with warnings treated as errors
- [ ] Core tests were added or updated for changes to schema migration, recipe
      invariants, Finder representation, the resolver, the queue, or persistence
- [ ] No em or en dashes, and no contractions
- [ ] No `Color.accentColor`; interface color comes from `RankFolderPalette`
- [ ] New behavior that changes what Rank & Folder reads, writes, or automates is
      reflected in `PRIVACY.md`, `SECURITY.md`, or `Docs/`
- [ ] `CHANGELOG.md` has an entry under Unreleased

## Permissions and files

<!-- State plainly whether this changes what Rank & Folder reads, writes, or
     automates, and whether it needs any macOS permission it did not need
     before. Write "no change" if that is the case. -->
